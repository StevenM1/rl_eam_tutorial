\usepackage[most]{tcolorbox}
\tcbset{
  colback=gray!10!white, % background color
  colframe=gray!80!black, % border color
  boxrule=0.5pt, % border thickness
  arc=4pt, % rounded corners
  left=6pt,
  right=6pt,
  top=6pt,
  bottom=6pt
}
% \lstset{
%   breaklines=true
% }